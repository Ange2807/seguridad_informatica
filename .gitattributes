# Auto detect text files and perform LF normalization
* text=auto

# Shell scripts must always use LF, even when checked out on Windows,
# otherwise the shebang breaks inside Linux containers.
*.sh text eol=lf
